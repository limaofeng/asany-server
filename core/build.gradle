description = "主框架 - 框架主体,包含基础的集成框架"

configurations {
    compile.exclude module: "tomcat-jdbc"
}

dependencies {
    compile "org.wltea.ik-analyzer:ik-analyzer:7.7.2"
    compile "com.hankcs:hanlp:portable-1.7.1"
}